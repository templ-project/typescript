{
  "servers": {
    "github": {
      "url": "https://api.githubcopilot.com/mcp/",
      "type": "http"
    },
    "ts-language-server": {
      "command": "${userHome}/go/bin/mcp-language-server",
      "args": ["--workspace", "${workspaceFolder}", "--lsp", "typescript-language-server", "--", "--stdio"],
      "env": {
        "LOG_LEVEL": "DEBUG"
      }
    }
  }
}
